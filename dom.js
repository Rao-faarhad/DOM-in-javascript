// thr dom represent a document with a logical TreeWalkerit allows us to manipulating webpage content html elements
// getElementById
// getElementByClassName
let smallImage= document.getElementsByClassName("img1");
// smallImage[1].src ="give link which we can change";
for(let i =0; i< smallImage.length; i++) {
console.dir(smallImage[i].src);
}

// if we want to select all tags like p,h etc
document.getElementsByTagName("p");

//QuerySelector allow us to use css selector;
document.querySelector('p');
document.querySelector('#myId');
document.querySelector('.myClass');
document.querySelector('h1');
// if you want to select All
document.querySelectorAll('h1');

// mainpulating Attributes

// Obj.getAttribute(attr)

// Obj.getAttribute(attr,val)

//classList.add( to add new class)
//classList.remove(to remove clsss)
//classList.contains(to check class exist)
//classList.toggle(to toggle btween add/remove)


















































































































































































































































































































































































































































































































































































































































































































































































































































































































